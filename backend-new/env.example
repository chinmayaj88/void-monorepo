# ============================================
# Void Cloud Drive - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# cp env.example .env

# ============================================
# Application Configuration
# ============================================
NODE_ENV=development
PORT=3000
APP_NAME=Void Cloud Drive
APP_URL=http://localhost:3000
FRONTEND_URL=http://localhost:3000
CORS_ORIGIN=http://localhost:3000

# ============================================
# Database Configuration
# ============================================
DATABASE_URL=postgresql://user:password@localhost:5432/void_db
DB_MAX_CONNECTIONS=20
DB_CONNECTION_TIMEOUT=5000

# ============================================
# JWT Configuration
# ============================================
# JWT_SECRET must be at least 32 characters long
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long-change-this-in-production
JWT_ACCESS_TOKEN_EXPIRY=15m
JWT_REFRESH_TOKEN_EXPIRY=7d

# ============================================
# Authentication & Security
# ============================================
# Maximum number of devices per user
MAX_DEVICES_PER_USER=3

# Admin email for initial setup
ADMIN_EMAIL=admin@example.com

# IP Whitelisting (optional - set to 'true' to enforce)
# When enabled, only IPs in the whitelist can login
ENFORCE_IP_WHITELIST=false

# TOTP Configuration
TOTP_ISSUER=Void Cloud Drive

# ============================================
# Email Configuration (SMTP)
# ============================================
# Set SMTP_ENABLED=true to enable email sending
# In development, emails are logged to console when disabled
SMTP_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_ADDRESS=no-reply@void.com

# ============================================
# OCI (Oracle Cloud Infrastructure) Storage
# ============================================
# Required for file storage
OCI_TENANCY_ID=ocid1.tenancy.oc1..
OCI_USER_ID=ocid1.user.oc1..
OCI_FINGERPRINT=xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx:xx
OCI_PRIVATE_KEY=-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----
OCI_NAMESPACE=your-namespace
OCI_BUCKET_NAME=your-bucket-name
OCI_COMPARTMENT_ID=ocid1.compartment.oc1..

# ============================================
# Notes
# ============================================
# - JWT_SECRET: Generate a strong random secret (at least 32 characters)
#   You can generate one with: openssl rand -base64 32
#
# - SMTP Configuration: For Gmail, you need to use an App Password
#   For other providers, adjust SMTP_HOST and SMTP_PORT accordingly
#
# - ENFORCE_IP_WHITELIST: Set to 'true' only if you want to restrict
#   logins to whitelisted IP addresses (useful for family/private deployments)
#
# - MAX_DEVICES_PER_USER: Maximum number of devices a user can register
#   Default is 3, adjust based on your needs
#
# - JWT Token Expiry:
#   - JWT_ACCESS_TOKEN_EXPIRY: Format like '15m', '1h', '30s'
#   - JWT_REFRESH_TOKEN_EXPIRY: Format like '7d', '30d', '1w'

